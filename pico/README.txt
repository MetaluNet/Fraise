Fraise modules and build system for the Raspberry Pico processor (rp2040).

The pico_sdk (and arm toolchain) must be installed somewhere, and the PICO_SDK_PATH environment variable
must be set accordingly.


